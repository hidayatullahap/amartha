meta {
  name: [Account] Login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/account/login
  body: json
  auth: inherit
}

body:json {
  {
    "username":"user1"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
